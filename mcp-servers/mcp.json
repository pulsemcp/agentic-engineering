{
  "cw-log-analyzer": {
    "title": "Log Analyzer with CloudWatch Logs",
    "description": "Use when debugging or investigating issues that would likely benefit from CloudWatch log introspection. Search and filter CloudWatch Logs, find error patterns, and correlate log events across multiple services.",
    "type": "stdio",
    "command": "uvx",
    "args": [
      "--from",
      "git+https://github.com/awslabs/Log-Analyzer-with-MCP",
      "cw-mcp-server"
    ],
    "env": {
      "AWS_DEFAULT_REGION": "us-east-1",
      "AWS_ACCESS_KEY_ID": "${AWS_ACCESS_KEY_ID}",
      "AWS_SECRET_ACCESS_KEY": "${AWS_SECRET_ACCESS_KEY}"
    }
  },
  "pg-postgres-mcp-ro": {
    "title": "PostgreSQL MCP Pro (Read-Only)",
    "description": "PostgreSQL tuning and analysis server. Provides index tuning, explain plans, health checks, and safe SQL execution. Read-only with resource limits.",
    "type": "stdio",
    "command": "uvx",
    "args": [
      "--from",
      "postgres-mcp==0.3.0",
      "postgres-mcp",
      "--access-mode=restricted"
    ],
    "env": {
      "DATABASE_URI": "postgresql://${PG_USER}:${PG_PASSWORD}@${PG_HOST}:${PG_PORT}/${PG_DATABASE}"
    }
  },
  "pg-postgres-mcp-rw": {
    "title": "PostgreSQL MCP Pro (Read-Write)",
    "description": "PostgreSQL tuning and analysis server. Provides index tuning, explain plans, health checks, and safe SQL execution. Full read/write access for development.",
    "type": "stdio",
    "command": "uvx",
    "args": [
      "--from",
      "postgres-mcp==0.3.0",
      "postgres-mcp",
      "--access-mode=unrestricted"
    ],
    "env": {
      "DATABASE_URI": "postgresql://${PG_USER}:${PG_PASSWORD}@${PG_HOST}:${PG_PORT}/${PG_DATABASE}"
    }
  },
  "aws-iac": {
    "title": "AWS Infrastructure as Code",
    "description": "Use when building or troubleshooting AWS infrastructure as code. Validate CloudFormation templates, check compliance, search CDK documentation, get best practices guidance, and debug deployment failures.",
    "type": "stdio",
    "command": "uvx",
    "args": [
      "awslabs.aws-iac-mcp-server==1.0.11"
    ],
    "env": {
      "AWS_PROFILE": "${AWS_PROFILE}",
      "AWS_DEFAULT_REGION": "${AWS_DEFAULT_REGION}",
      "AWS_ACCESS_KEY_ID": "${AWS_ACCESS_KEY_ID}",
      "AWS_SECRET_ACCESS_KEY": "${AWS_SECRET_ACCESS_KEY}",
      "AWS_SESSION_TOKEN": "${AWS_SESSION_TOKEN}",
      "FASTMCP_LOG_LEVEL": "ERROR"
    }
  },
  "google-sheets": {
    "title": "Google Sheets",
    "description": "Use when you need to read, create, or modify Google Sheets spreadsheets. Search for spreadsheets, read/write cell data, manage sheet tabs, add rows/columns, apply batch updates, share spreadsheets, and browse Drive folders.",
    "type": "stdio",
    "command": "uvx",
    "args": [
      "mcp-google-sheets==0.6.0"
    ],
    "env": {
      "CREDENTIALS_CONFIG": "${GOOGLE_SHEETS_CREDENTIALS_CONFIG}",
      "DRIVE_FOLDER_ID": "${GOOGLE_SHEETS_DRIVE_FOLDER_ID}"
    }
  }
}
